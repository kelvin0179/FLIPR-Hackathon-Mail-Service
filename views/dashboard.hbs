{{!-- <h1>Welcome {{name}}</h1>
<a class="btn btn-danger" href="/user/logout">Logout</a> --}}
<h2>Welcome {{name}}</h2>

{{#if response}}
<table class="striped">
    <thead>
        <tr>
            <th>Title</th>
            <th>Date</th>
            <th>Time</th>
            <th>Unit</th>
            <th>To</th>
        </tr>
    </thead>
    <tbody>
        {{#each response}}
        <tr>
            <td><a href="/api/notify/show/{{_id}}">{{subject}}</a></td>
            <td>{{formatDate date "MMMM Do YYYY, h:mm:ss a"}}</td>
            {{!-- the formatDate here acts as a function in helpers/hbs through app.js to
            format date. The rest two attributes are passed as parameters --}}
            <td><span class="dash-status">{{schTime}}</span></td>
            <td><span class="dash-status">{{schUnit}}</span></td>
            <td><span class="dash-status">{{to}}</span></td>
            <td>
                {{!-- <a href="/stories/edit/{{{_id}}}" class="btn btn-float"><i class="fa fa-edit"></i></a> --}}
                <a href="/api/notify/schedule/{{_id}}" class="btn btn-float blue">
                    <i class="fas fa-envelope-square"></i>
                </a>
                <a href="/api/notify/edit/{{_id}}" class="btn btn-float">
                    <i class="fas fa-edit"></i>
                </a>
                <form action="/api/notify/{{_id}}" method="POST" id="delete-form">
                    <input type="hidden" name="_method" value="DELETE">
                    <button type="submit" class="btn btn-float red">
                        <i class="fa fa-trash"></i>
                    </button>
                </form>
            </td>
        </tr>
        {{/each}}
    </tbody>
</table>
{{else}}
<h5>You have not created any Mails</h5>
{{/if}}